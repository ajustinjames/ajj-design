{
  "name": "ajj-design",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "tokens:build": "pnpm -r --filter './packages/*-tokens' build",
    "prestorybook": "pnpm tokens:build",
    "prebuild-storybook": "pnpm tokens:build",
    "test": "pnpm -r --filter './packages/*-components' test",
    "test:screenshots": "pnpm -r --filter './packages/*-components' test:screenshots",
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@storybook/web-components": "^10.3.5",
    "@storybook/web-components-vite": "^10.0.0",
    "storybook": "^10.0.0",
    "typescript": "^5.4.0"
  }
}
